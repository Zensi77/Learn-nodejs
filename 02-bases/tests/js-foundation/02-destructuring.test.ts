import { characters } from '../../src/js-foundation/02-destructuring';

describe("Destructuring", () => {
  test("should contains Batman", () => {
    // expect(characters).toContain("Batman");
  });

  test("Flash first, Superman second", () => {
    // const [flash, superman] = characters; // Destructuro el array characters y obtengo el valor de las variables flash y superman
    // expect(flash).toBe("Flash");
    // expect(superman).toBe("Superman");
  });
});
